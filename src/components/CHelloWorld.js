import { VContainer, VBtn } from "vuetify/lib/components"

export default {
  name: "CelloWorld",
  data: () => ({}),
  render(h) {
    return (
      <VContainer>
        <VBtn>Milad</VBtn>
      </VContainer>
    )
  }
}
